import 'dart:io';

class ImageModel {
    bool? isUploaded;
     bool? uploading;
      File? imageFile;
    String ? imageUrl;

  ImageModel({ isUploaded,
     uploading,
     imageFile,
      imageUrl,}

  );
}